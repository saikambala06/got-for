// Reserved for future auto-detection of job postings on supported sites
// (e.g. LinkedIn, Indeed, Greenhouse). Currently a no-op so the extension
// installs cleanly; the popup handles manual capture for now.
